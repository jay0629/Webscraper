To run the program (Linux only for now) install the scrapy libraries by the
following command:

sudo pip install scrapy
chmod a+x scraper.py
./scraper.py

see scrapy output:
scrape_out

see a little grep cleaning of output file in:
exampleMovies

